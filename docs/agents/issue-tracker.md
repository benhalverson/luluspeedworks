# Issue tracker

GitHub repository: `benhalverson/luluspeedworks`.
Read an issue and its comments using `gh issue view NUMBER --repo
benhalverson/luluspeedworks --json title,body,comments`.
Issue #1 is the catalog scope; `docs/catalog-contract.md` records the verified
contract, decisions and live acceptance dependencies. Issue #2 owns product
selection, links, colors and quantities. No backend or deployment is in #1.
